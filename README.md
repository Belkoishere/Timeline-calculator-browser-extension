A browser extension that allows the user to generate a timetable for an assignment after the deadline is entered.

By having the application as a browser extension the user can view their assignement in-browser at the same time as the application.
